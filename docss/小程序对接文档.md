# 万能下载器小程序对接文档

这份文档只面向小程序前端对接。

目标很简单：

1. 用户粘贴抖音分享文案
2. 小程序调后端解析
3. 展示作品详情和可下载资源
4. 调后端生成下载票据
5. 小程序下载并保存到相册

## 1. 基础信息

### 服务地址

开发环境示例：

`http://127.0.0.1:8787`

正式环境请替换成你自己的 HTTPS 域名。

小程序上线前要确保：

1. `request` 合法域名已配置
2. `downloadFile` 合法域名已配置
3. 如果走 HTTPS，证书可用

## 2. 推荐对接流程

### 最短主链路

```text
用户粘贴分享文案
  -> POST /api/parse
  -> 拿到 awemeId 和基础结果
  -> POST /api/detail
  -> 拿到标题、作者、封面、资源列表
  -> 用户选择资源
  -> POST /api/download/prepare
  -> 拿到 downloadUrl
  -> wx.downloadFile(downloadUrl)
  -> wx.saveVideoToPhotosAlbum / wx.saveImageToPhotosAlbum
```

### 更省请求的版本

如果你想少一次接口调用：

1. 调 `POST /api/parse`
2. 请求体里加 `fetchMetadata: true`
3. 后端会在解析成功后顺手把详情也补上

这个模式更适合首页直接跳结果页。

## 3. 接口清单

### 3.1 解析分享文案

`POST /api/parse`

#### 请求体

```json
{
  "text": "3.87 复制打开抖音，看看【小野猫史(日更版)的作品】当同事天天作秀内卷 #猫meme #小野猫史 https://v.douyin.com/oCzaAxEP5vk/ M@W.ZM GvF:/ 04/16",
  "resolveRedirect": true,
  "includeDebug": false,
  "fetchMetadata": false
}
```

#### 字段说明

- `text`: 必填，用户粘贴的完整分享文案
- `resolveRedirect`: 可选，建议传 `true`
- `includeDebug`: 可选，正式环境建议 `false`
- `fetchMetadata`: 可选
  - `false`: 只做文案解析
  - `true`: 解析成功后顺手抓作品详情

#### 成功返回

```json
{
  "success": true,
  "requestId": "98b545e7-64b0-4c40-a8e4-0f853d4f9725",
  "data": {
    "parseId": "parse_1777420801234_d9e8f7a6",
    "platform": "douyin",
    "type": "video",
    "input": {
      "rawText": "3.87 复制打开抖音，看看【...】 https://v.douyin.com/oCzaAxEP5vk/",
      "shareCode": "3.87",
      "shareUrl": "https://v.douyin.com/oCzaAxEP5vk/"
    },
    "resolved": {
      "finalUrl": "https://www.douyin.com/video/7633816932935879945?previous_page=app_code_link",
      "awemeId": "7633816932935879945",
      "title": "小野猫史(日更版)的作品",
      "resourceTypeHint": "video"
    },
    "status": "resolved",
    "nextStep": "可以继续调用作品详情抓取模块"
  }
}
```

#### 小程序最关心的字段

- `data.resolved.awemeId`
- `data.resolved.finalUrl`
- `data.resolved.resourceTypeHint`
- `data.status`

#### 状态说明

- `partial`: 只解析出了链接，还没拿到作品 ID
- `resolved`: 已拿到作品 ID，可以继续调详情接口

### 3.2 抓取作品详情

`POST /api/detail`

#### 请求体

```json
{
  "awemeId": "7346171450493127975",
  "resolvedUrl": "https://www.douyin.com/video/7346171450493127975",
  "typeHint": "video",
  "includeDebug": false
}
```

#### 字段说明

- `awemeId`: 必填
- `resolvedUrl`: 可选，建议把解析接口返回的 `finalUrl` 一并传回来
- `typeHint`: 可选，建议传 `video` 或 `note`
- `includeDebug`: 可选

#### 视频作品返回示例

```json
{
  "success": true,
  "requestId": "80459758-5ac8-4c36-bd8e-2c3c3e489d96",
  "data": {
    "awemeId": "7346171450493127975",
    "mediaType": "video",
    "title": "TikTok短剧怎么挂链接？[2.0版] #短剧推广 #短剧出海 #海外短剧分销 #短剧",
    "description": "TikTok短剧怎么挂链接？[2.0版] #短剧推广 #短剧出海 #海外短剧分销 #短剧",
    "author": {
      "uid": "",
      "secUid": "MS4wLjABAAAAcY2DLlaGPgF2r7vdz2M5LuKYz7_nKzWJiwjP9nfxu_I",
      "nickname": "傻瓜推文",
      "avatar": "https://p11.douyinpic.com/..."
    },
    "cover": "https://p11-sign.douyinpic.com/...",
    "durationMs": 96712,
    "statistics": {
      "commentCount": 16,
      "diggCount": 191,
      "playCount": 0,
      "shareCount": 63,
      "collectCount": 179
    },
    "sharePage": {
      "sourceUrl": "https://www.iesdouyin.com/share/video/7346171450493127975/",
      "pageUrl": "https://www.iesdouyin.com/share/video/7346171450493127975/"
    },
    "sources": [
      {
        "id": "wm_default",
        "kind": "video",
        "label": "默认源",
        "watermark": "with_watermark",
        "url": "https://aweme.snssdk.com/aweme/v1/playwm/?video_id=...",
        "width": 576,
        "height": 1024,
        "durationMs": 96712
      },
      {
        "id": "guess_nowm_default",
        "kind": "video",
        "label": "默认源(推测无水印)",
        "watermark": "unknown",
        "url": "https://aweme.snssdk.com/aweme/v1/play/?video_id=...",
        "width": 576,
        "height": 1024,
        "durationMs": 96712
      }
    ],
    "images": [],
    "rawType": 4
  }
}
```

#### 图文作品返回示例

```json
{
  "success": true,
  "data": {
    "awemeId": "7483075097301994809",
    "mediaType": "note",
    "title": "黄花风铃木绽放，南宁便成了金色的海洋。",
    "author": {
      "nickname": "作者名",
      "avatar": "https://..."
    },
    "cover": "https://...",
    "sources": [],
    "images": [
      {
        "id": "image_1",
        "url": "https://...",
        "downloadUrl": "https://...",
        "width": 1440,
        "height": 1921
      }
    ]
  }
}
```

#### 小程序最关心的字段

- `data.mediaType`
- `data.title`
- `data.author.nickname`
- `data.cover`
- `data.sources`
- `data.images`

#### 前端展示建议

如果 `mediaType === "video"`：

1. 显示封面
2. 显示标题、作者、时长
3. 把 `sources` 渲染成下拉或按钮组

如果 `mediaType === "note"`：

1. 显示封面和标题
2. 把 `images` 渲染成可选择列表
3. 默认选第一张图

### 3.3 生成下载票据

`POST /api/download/prepare`

#### 视频请求体

```json
{
  "awemeId": "7346171450493127975",
  "typeHint": "video",
  "sourceId": "wm_default"
}
```

#### 图文请求体

```json
{
  "awemeId": "7483075097301994809",
  "typeHint": "note",
  "imageId": "image_1"
}
```

#### 字段说明

- `awemeId`: 必填
- `typeHint`: 建议传 `video` 或 `note`
- `sourceId`: 下载视频时传
- `imageId`: 下载图片时传

#### 返回示例

```json
{
  "success": true,
  "requestId": "1bbac3ff-3207-4bbf-8e8b-3ec0c9be8930",
  "data": {
    "ticket": "dl_7a82e4a23a0246ad926e9abdb6bcbe7a",
    "awemeId": "7346171450493127975",
    "mediaType": "video",
    "assetType": "video",
    "assetId": "wm_default",
    "fileName": "TikTok短剧怎么挂链接？[2.0版] #短剧推广 #短剧出海 #海外短剧分销 #短剧-默认源.mp4",
    "expiresAt": 1777434000000,
    "downloadUrl": "http://127.0.0.1:8787/api/download/file/dl_7a82e4a23a0246ad926e9abdb6bcbe7a",
    "source": {
      "label": "默认源",
      "contentTypeHint": "video/mp4"
    }
  }
}
```

#### 小程序最关心的字段

- `data.downloadUrl`
- `data.fileName`
- `data.assetType`
- `data.expiresAt`

### 3.4 中转下载地址

`GET /api/download/file/:ticket`

这个接口不用你手写调用，直接交给：

- `wx.downloadFile`

来请求就行。

## 4. 小程序端代码示例

### 4.1 解析分享文案

```js
function parseShareText(text) {
  return new Promise((resolve, reject) => {
    wx.request({
      url: `${BASE_URL}/api/parse`,
      method: "POST",
      data: {
        text,
        resolveRedirect: true,
        includeDebug: false,
        fetchMetadata: false,
      },
      success(res) {
        if (res.statusCode === 200 && res.data.success) {
          resolve(res.data.data);
          return;
        }

        reject(new Error(res.data?.error?.message || "解析失败"));
      },
      fail(err) {
        reject(err);
      },
    });
  });
}
```

### 4.2 获取作品详情

```js
function fetchDetail(parseResult) {
  return new Promise((resolve, reject) => {
    wx.request({
      url: `${BASE_URL}/api/detail`,
      method: "POST",
      data: {
        awemeId: parseResult.resolved.awemeId,
        resolvedUrl: parseResult.resolved.finalUrl,
        typeHint: parseResult.resolved.resourceTypeHint,
        includeDebug: false,
      },
      success(res) {
        if (res.statusCode === 200 && res.data.success) {
          resolve(res.data.data);
          return;
        }

        reject(new Error(res.data?.error?.message || "获取详情失败"));
      },
      fail(err) {
        reject(err);
      },
    });
  });
}
```

### 4.3 生成下载票据

```js
function prepareDownload(detail, selectedId) {
  const data =
    detail.mediaType === "video"
      ? {
          awemeId: detail.awemeId,
          typeHint: "video",
          sourceId: selectedId,
        }
      : {
          awemeId: detail.awemeId,
          typeHint: "note",
          imageId: selectedId,
        };

  return new Promise((resolve, reject) => {
    wx.request({
      url: `${BASE_URL}/api/download/prepare`,
      method: "POST",
      data,
      success(res) {
        if (res.statusCode === 200 && res.data.success) {
          resolve(res.data.data);
          return;
        }

        reject(new Error(res.data?.error?.message || "生成下载地址失败"));
      },
      fail(err) {
        reject(err);
      },
    });
  });
}
```

### 4.4 下载并保存视频

```js
function downloadAndSaveVideo(downloadUrl) {
  return new Promise((resolve, reject) => {
    wx.downloadFile({
      url: downloadUrl,
      success(downloadRes) {
        if (downloadRes.statusCode !== 200) {
          reject(new Error("下载失败"));
          return;
        }

        wx.saveVideoToPhotosAlbum({
          filePath: downloadRes.tempFilePath,
          success() {
            resolve();
          },
          fail(err) {
            reject(err);
          },
        });
      },
      fail(err) {
        reject(err);
      },
    });
  });
}
```

### 4.5 下载并保存图片

```js
function downloadAndSaveImage(downloadUrl) {
  return new Promise((resolve, reject) => {
    wx.downloadFile({
      url: downloadUrl,
      success(downloadRes) {
        if (downloadRes.statusCode !== 200) {
          reject(new Error("下载失败"));
          return;
        }

        wx.saveImageToPhotosAlbum({
          filePath: downloadRes.tempFilePath,
          success() {
            resolve();
          },
          fail(err) {
            reject(err);
          },
        });
      },
      fail(err) {
        reject(err);
      },
    });
  });
}
```

## 5. 推荐前端状态流转

```text
idle
 -> parsing
 -> parse_success
 -> detail_loading
 -> detail_success
 -> prepare_download
 -> downloading
 -> save_success
 -> error
```

### 页面建议字段

```js
data: {
  inputText: "",
  parseResult: null,
  detailResult: null,
  selectedSourceId: "",
  selectedImageId: "",
  downloadTicket: null,
  loading: false,
  loadingText: "",
}
```

## 6. 页面逻辑建议

### 首页点击“开始提取”

建议顺序：

1. 校验输入不为空
2. 调 `parse`
3. 如果 `parse.status !== "resolved"`，提示用户重新复制分享文案
4. 调 `detail`
5. 根据 `mediaType` 初始化默认选中资源

### 点击“保存视频”或“保存图片”

建议顺序：

1. 调 `download/prepare`
2. 拿到 `downloadUrl`
3. 调 `wx.downloadFile`
4. 视频走 `wx.saveVideoToPhotosAlbum`
5. 图片走 `wx.saveImageToPhotosAlbum`

## 7. 常见错误处理

### 解析失败

后端可能返回：

- `EMPTY_TEXT`
- `NO_URL_FOUND`
- `UNSUPPORTED_PLATFORM`

前端文案建议：

- `EMPTY_TEXT`: 请输入抖音分享文案
- `NO_URL_FOUND`: 没找到可解析链接，请重新复制完整分享文案
- `UNSUPPORTED_PLATFORM`: 当前仅支持抖音分享链接

### 详情失败

后端可能返回：

- `CONTENT_UNAVAILABLE`
- `DETAIL_FETCH_FAILED`
- `ROUTER_DATA_NOT_FOUND`

前端文案建议：

- 作品不存在、已删除或权限受限
- 当前作品暂时无法解析，请稍后重试

### 下载失败

后端可能返回：

- `TICKET_NOT_FOUND`
- `TICKET_EXPIRED`
- `DOWNLOAD_UPSTREAM_FAILED`

前端文案建议：

- 下载地址已过期，请重新点击保存
- 当前下载源暂时不可用，请切换资源或稍后重试

## 8. 权限处理

保存到相册前，小程序要处理相册权限。

视频：

- `wx.saveVideoToPhotosAlbum`

图片：

- `wx.saveImageToPhotosAlbum`

如果用户拒绝授权，建议：

1. 提示用户去设置里开启相册权限
2. 不要静默失败

## 9. 字段对接速查

### 结果页展示

- 标题：`detailResult.title`
- 作者：`detailResult.author.nickname`
- 封面：`detailResult.cover`
- 视频列表：`detailResult.sources`
- 图片列表：`detailResult.images`

### 默认选中资源

视频：

- 默认选 `detailResult.sources[0].id`

图片：

- 默认选 `detailResult.images[0].id`

### 下载调用

视频：

```js
{
  awemeId: detailResult.awemeId,
  typeHint: "video",
  sourceId: selectedSourceId
}
```

图片：

```js
{
  awemeId: detailResult.awemeId,
  typeHint: "note",
  imageId: selectedImageId
}
```

## 10. 当前版本注意事项

这几个点前端同学需要知道：

1. 下载票据默认 10 分钟过期
2. 当前票据存在内存里，服务重启后旧票据会失效
3. 当前没有登录态和用户隔离，不要直接拿这版当生产最终形态
4. `guess_nowm_default` 是“推测无水印源”，不是强保证

## 11. 推荐你们小程序先这样接

如果你想先最快跑通：

1. 首页只做输入框和“开始提取”
2. 调 `parse`
3. 成功后调 `detail`
4. 结果页只显示一个默认资源
5. 直接调 `download/prepare`
6. 下载并保存

先别急着上多源切换、历史记录、复杂教程。

先把主链路跑活。
