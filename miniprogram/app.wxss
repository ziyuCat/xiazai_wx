page {
  min-height: 100%;
  background: #f2f2f7;
  color: #0f172a;
}

view,
text,
button,
input,
textarea {
  box-sizing: border-box;
}

button {
  background: transparent;
  border: none;
  border-radius: 0;
  line-height: 1.2;
  padding: 0;
  margin: 0;
}

button::after {
  border: none;
}
